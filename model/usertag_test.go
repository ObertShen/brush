package model

import "testing"

func TestUserTag(t *testing.T) {
	// db.GetInstance().OpenConnect()

	// assert := assert.New(t)
	// assert.NotPanics(func() { userTagDataIns = NewUserTagData() })

	// testRecord := &UserTag{
	// 	Platform:  "weibo",
	// 	UserID:    10,
	// 	UnionID:   util.GetUUID(),
	// 	Tags: append([]nameAndValue{}, nameAndValue{Name: "test", Value: "test"});
	// 	UpdatedAt: time.Now().Unix(),
	// 	CreatedAt: time.Now().Unix(),
	// }

	// affected, err := UserDataIns.Insert(testRecord)
	// if assert.NoError(err) {
	// 	assert.NotZero(affected)
	// }

	// has, err := UserDataIns.Get(&User{ID: testRecord.ID})
	// if assert.NoError(err) {
	// 	assert.True(has)
	// }

	// userList, err := UserDataIns.GetList(&User{ID: testRecord.ID})
	// if assert.NoError(err) {
	// 	assert.True(len(userList) == 1)
	// }
}
